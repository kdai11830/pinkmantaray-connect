<!DOCTYPE html>
<html lang="en">
<head>
<title>Notifications | Pinkmantaray Connect</title>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link href="../stylesheets/notifications.css" rel="stylesheet" type="text/css">
<link
	href="https://fonts.googleapis.com/css2?family=Montserrat:wght@100;400;600;800;900&family=Open+Sans&family=Poppins:wght@400;700;900&family=Roboto:ital,wght@0,100;0,300;0,500;0,900;1,900&display=swap"
	rel="stylesheet">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script type="text/javascript" src="/socket.io/socket.io.js"></script> 
<script type="text/javascript" src="https://code.jquery.com/ui/1.12.0/jquery-ui.min.js"></script>
<script type="text/javascript" src="/scripts/notificationsScript.js"></script>

</head>

<body>

<%- include("../partials/header") %>
<div>
	<div class="postcard">
		<div class="notif-header">Requests</div>
		<table id="invitationsTable">
			<% var counter = 0; %>
			<% for (var i in data['invitations']) { %>
			<tr id="<%= counter %>">
				<td id="buttons<%= counter %>" class="buttonParent">
					<button class="acceptBtn" id="accept<%= counter %>">✓</button>
					<button class="ignoreBtn" id="ignore<%= counter %>">✕</button>
				</td>
				<td><%= data['invitations'][i].pronouns %></td>
				<td><%= data['invitations'][i].country %></td>
				<td><%= new Date().getFullYear() - data['invitations'][i].year %></td>
				<td><%= data['invitations'][i].gender.join(', ') %></td>
				<td><%= data['invitations'][i].sexuality.join(', ') %></td>
				<td><%= data['invitations'][i].race_ethnicity.join(', ') %></td>
				<td><%= data['invitations'][i].religion.join(', ') %></td>
				<td><%= data['invitations'][i].interest.join(', ') %></td>
			</tr>
			<% counter++; %>
			<% } %>
		</table>
	</div>
	
	<div class="postcard">
		<div class="notif-header">Pending</div>
		<table id="pendingTable">
			<% var counter = 0; %>
			<% for (var i in data['pending']) { %>
		
			<tr id="<%= counter %>">
				<td id="buttons<%= counter %>" class="buttonParent">
					<button class="cancelBtn" id="cancel<%= counter %>">✕</button>
				</td>
				<td><%= data['pending'][i].pronouns %></td>
				<td><%= data['pending'][i].country %></td>
				<td><%= new Date().getFullYear() - data['pending'][i].year %></td>
				<td><%= data['pending'][i].gender.join(', ') %></td>
				<td><%= data['pending'][i].sexuality.join(', ') %></td>
				<td><%= data['pending'][i].race_ethnicity.join(', ') %></td>
				<td><%= data['pending'][i].religion.join(', ') %></td>
				<td><%= data['pending'][i].interest.join(', ') %></td>
			</tr>
			<!-- <tr id="buttons<%= counter %>">
							<td><button class="cancelBtn" id="cancel<%= counter %>">Cancel</button></td>
						</tr> -->
			<% counter++; %>
			<% } %>
		
		</table>
	</div>
	
</div>

</body>